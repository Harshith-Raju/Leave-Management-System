{
  "name": "leave-management-system",
  "version": "1.0.0",
  "description": "A mini leave management system for startups",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "init-db": "node scripts/initDB.js",
    "seed-demo": "node -e \"require('dotenv').config(); const { connectToDatabase } = require('./config/database'); const { seedDemo } = require('./scripts/seedDemo'); connectToDatabase().then(() => seedDemo()).then(() => { console.log('Demo admin seeded'); process.exit(0); }).catch((e) => { console.error(e); process.exit(1); });\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
